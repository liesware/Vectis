[project]
name = "vectis-tests"
version = "0.0.0"
description = "Test and tooling scripts for Vectis (HTTP suites, fuzzing)."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
fuzz = [
    "schemathesis>=4,<5",
]

[tool.uv]
package = false
