name: Release Binaries

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-binaries-${{ github.ref }}
  cancel-in-progress: false

jobs:
  metadata:
    name: Validate release metadata
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      version: ${{ steps.metadata.outputs.version }}
      release_tag: ${{ steps.metadata.outputs.release_tag }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: Read release metadata
        id: metadata
        shell: bash
        run: |
          set -euo pipefail
          version="$(cargo metadata --locked --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "vectis") | .version')"
          test -n "$version"
          test "$version" != "null"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "release_tag=v$version" >> "$GITHUB_OUTPUT"

          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            test "$GITHUB_REF_TYPE" = "tag"
            test "$GITHUB_REF_NAME" = "v$version"
          fi

  build-binary:
    name: Build ${{ matrix.archive }}
    needs: metadata
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            archive: vectis-linux-amd64
          - runner: ubuntu-24.04-arm
            archive: vectis-linux-arm64
          - runner: macos-14
            archive: vectis-macos-arm64
    env:
      CARGO_TERM_COLOR: always
      RELEASE_TAG: ${{ needs.metadata.outputs.release_tag }}
      PACKAGE_FILES: vectis LICENSE NOTICE README.md SECURITY.md

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: Build release binary
        run: cargo build --release --locked

      - name: Smoke test release binary
        run: target/release/vectis version

      - name: Package binary
        shell: bash
        run: |
          set -euo pipefail
          package_dir="${{ matrix.archive }}-${RELEASE_TAG}"
          asset="${package_dir}.tar.gz"
          mkdir -p "dist/${package_dir}"
          for file in $PACKAGE_FILES; do
            if [ "$file" = vectis ]; then
              install -m 755 target/release/vectis "dist/${package_dir}/vectis"
            else
              cp "$file" "dist/${package_dir}/"
            fi
          done
          tar -C dist -czf "dist/${asset}" "$package_dir"

      - name: Verify release package contents
        shell: bash
        run: |
          set -euo pipefail
          package_dir="${{ matrix.archive }}-${RELEASE_TAG}"
          asset="dist/${package_dir}.tar.gz"
          expected="$(mktemp)"
          actual="$(mktemp)"
          trap 'rm -f "$expected" "$actual"' EXIT

          {
            printf '%s/\n' "$package_dir"
            for file in $PACKAGE_FILES; do
              printf '%s/%s\n' "$package_dir" "$file"
            done
          } | LC_ALL=C sort > "$expected"
          tar -tzf "$asset" | LC_ALL=C sort > "$actual"
          diff -u "$expected" "$actual"

      - name: Attest binary provenance
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4
        with:
          subject-path: dist/${{ matrix.archive }}-${{ env.RELEASE_TAG }}.tar.gz

      - name: Upload release asset
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-${{ matrix.archive }}
          path: dist/${{ matrix.archive }}-${{ env.RELEASE_TAG }}.tar.gz
          if-no-files-found: error
          retention-days: 7

  publish:
    name: Publish GitHub release
    needs: [metadata, build-binary]
    if: github.event_name == 'push' && github.ref_type == 'tag'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      id-token: write
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_TAG: ${{ needs.metadata.outputs.release_tag }}

    steps:
      - name: Download release assets
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: release-*
          path: dist
          merge-multiple: true

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.1.3

      - name: Show Cosign version
        run: cosign version

      - name: Generate checksums
        shell: bash
        run: |
          set -euo pipefail
          (
            cd dist
            LC_ALL=C sha256sum *.tar.gz | LC_ALL=C sort -k2 > SHA256SUMS
            sha256sum -c SHA256SUMS
          )

      - name: Sign checksums with Cosign
        shell: bash
        run: |
          set -euo pipefail
          cosign sign-blob \
            --yes \
            --bundle dist/SHA256SUMS.sigstore.json \
            dist/SHA256SUMS

      - name: Verify checksum signature
        shell: bash
        run: |
          set -euo pipefail
          identity="https://github.com/${GITHUB_REPOSITORY}/.github/workflows/release.yml@refs/tags/${RELEASE_TAG}"
          cosign verify-blob \
            --bundle dist/SHA256SUMS.sigstore.json \
            --certificate-identity "$identity" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            dist/SHA256SUMS

      - name: Create GitHub release
        shell: bash
        run: |
          gh release create "$RELEASE_TAG" \
            dist/*.tar.gz \
            dist/SHA256SUMS \
            dist/SHA256SUMS.sigstore.json \
            --title "Vectis $RELEASE_TAG" \
            --generate-notes
